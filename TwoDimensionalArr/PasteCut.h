#pragma once
#include <iostream>
using namespace std;

void PasteRow(int** array, int row, int columnsSize, int rowsSize)
{
	for (int i = 0; i < columnsSize; i++)
	{
		for (int j = 0; j < rowsSize; j++)
		{

		}
		int value;
		cin >> value;
		array[row][i] = value;
	}
}

void PasteColumn(int** array, int row, int columnsSize, int rowsSize)
{
	for (int i = 0; i < columnsSize; i++)
	{
		for (int j = 0; j < rowsSize; j++)
		{

		}
		int value;
		cin >> value;
		array[row][i] = value;
	}
}

void CutRow(int** array, int row, int columnsSize, int rowsSize)
{
	for (int i = 0; i < columnsSize; i++)
	{
		for (int j = 0; j < rowsSize; j++)
		{

		}
		int value;
		cin >> value;
		array[row][i] = value;
	}
}

void CutColumn(int** array, int row, int columnsSize, int rowsSize)
{
	for (int i = 0; i < columnsSize; i++)
	{
		for (int j = 0; j < rowsSize; j++)
		{

		}
		int value;
		cin >> value;
		array[row][i] = value;
	}
}
